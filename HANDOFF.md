# 인수인계 — 왜 이렇게 만들었나

결과물이 아니라 **판단의 근거와 깨진 자리**를 남긴다. 같은 데서 두 번 깨지지 말라고.

- 대상 → 6살, 한글 **완전 처음**, 하루 15분, **종이에 뽑아서** 함
- 저장소 → `themoka7/myChildrenKorStudy`, 브랜치 `claude/my-children-kor-study-3v1398`
- 뿌리 → 영어 자료(`themoka7/myChildrenEngStudy`)의 세 겹 구조를 그대로 가져왔다

---

## 1. 영어 자료에서 가져온 것 / 새로 만든 것

**그대로 가져온 것** — 과목이 바뀌어도 통했다.

- 세 겹 구조: 지도(아이) → 학습지(종이) → 목록(부모). **아이 화면과 관리 화면을 분리**
- `units.json` 단일 원본 + HTML 은 전부 생성물
- 도장·마당·색종이 (동기 부여 장치는 과목과 무관하다)
- 레이아웃 넘침을 **빌드 실패로** 잡기
- 부모용 안내를 **그대로 읽으면 되는 문장**으로 쓰기
- 한 번 깨진 것은 전부 검사 항목으로 승격

**새로 만든 것** — 영어에서 쓸 것이 없었다.

- **획순 데이터** (`tools/jamo.py`). 한글 교육의 핵심이고 영어에는 대응물이 없다
- **색칠 도안 36개** (`tools/art.py`). 6살은 읽기 전에 칠한다
- **양식 4종**. 영어의 6문항(단어연결·파닉스·O/X…)은 6살 한글에 안 맞는다
- **커리큘럼 정합성 검사** (`check.py` 8번). 한글은 자모로 풀리기 때문에
  「아직 안 배운 글자가 나오는가」를 기계가 볼 수 있다. 영어에서는 못 했던 것이다

**A4 3쪽(문제2+정답1) 은 버렸다.** 6살에게 세 장은 길고, 한글 따라쓰기는 정답이
명확해서 정답지가 필요 없다. 대신 **부모용 한 줄을 같은 장 맨 아래**에 뒀다.
따로 뽑게 하면 아무도 안 뽑는다.

---

## 2. 커리큘럼을 이 순서로 짠 이유

모음 → 자음 → 조합 → 받침 없는 낱말 → 받침 있는 낱말 → 문장.

- **모음이 먼저다.** 획이 적어서 연필 잡는 연습이 되고, 소리가 또렷해서 6살이
  구별하기 쉽다. 자음(ㄱ=기역)은 이름과 소리가 달라서 더 어렵다
- **3마당(조합)이 고비다.** 여기서 아이가 "글자는 조립하는 거구나"를 깨닫는다.
  못 읽는 칸이 많아도 다음 마당으로 넘기면 4마당부터 통글자 암기가 된다.
  그래서 학습지·지도·안내 세 군데에 같은 경고를 박아 뒀다
- **받침은 순한 것부터** (ㅇ ㄴ ㄹ ㅁ → ㅊ). `꽃`(ㅊ받침 + ㄲ초성)을 5마당 끝에 둔 이유다
- **공주 테마**는 아이가 좋아해서다. 동기 부여가 커리큘럼보다 중요한 나이다.
  낱말·도안·지도 전부 공주 쪽으로 골랐다 — 단, `받침 없는/있는` 이라는
  **문법 제약이 테마보다 우선**한다 (`check.py` 5번이 강제한다)

---

## 3. 값비싼 실수들

### 3.1 그림은 눈으로 봐야만 검증된다 ★★★
코드는 통과하는데 그림이 안 읽혔다. **우유**를 우유갑으로 그렸더니 집처럼 보였고
(빨대 꽂은 컵으로 바꿈), **공주**의 왕관이 뿔처럼 보였고, **구두**는 옆모습으로
세 번 그렸는데 전부 새·물개처럼 보였다 (위에서 내려다본 모양 + 리본으로 바꿈).
→ `tools/preview_art.py` 로 **한 장에 모아 놓고 반드시 눈으로 본다.**
→ 도안을 고쳤으면 다시 본다. 안 보고 넘어가면 44장에 그대로 실려 나간다.

### 3.2 `height:100%` 가 그림을 0으로 찌그러뜨렸다 ★★
예시 낱말 카드에 `svg{height:100%}` 를 줬는데 부모 높이가 `auto` 라 그림이
10px 로 눌렸다. 화면에서도 작아서 "원래 저런가" 하고 넘길 뻔했다.
→ 인쇄물의 그림에는 **너비를 못 박는다** (`width:40mm`).

### 3.3 획순 번호가 획을 덮었다 ★★
번호를 획 시작점에 그대로 얹으면 획이 가려서, 어디서 시작하는지 **더** 헷갈린다.
글자 중심에서 밀어내는 방식도 세로획 위끝에서는 획을 따라 밀렸다.
→ 지금은 **획이 나아가는 반대쪽**에 놓는다. 그 방향은 그 획이 지나가지 않는다.
→ ㅅ·ㅈ·ㅊ 처럼 두 획이 같은 점에서 출발하는 자모만 `jamo.py` 에 좌표를 직접 적었다.

### 3.4 조합표 음절이 글꼴 서브셋에서 통째로 빠졌다 ★★★
`갸`·`뇨`·`듀` 같은 글자는 `units.json` 에도 소스에도 없다 — **빌드할 때 만들어진다.**
소스만 긁어서 서브셋을 만들었더니 57자가 빠졌고, 그대로 나갔으면 조합표가
두부(□)로 인쇄될 뻔했다.
→ `fonts.py` 가 학습지와 **같은 방법으로** 음절을 만들어 서브셋에 넣는다.
→ 그리고 `fonts.py --check` 가 생성된 HTML 을 훑어 빠진 글자를 잡는다. 이게 실제로 잡았다.

### 3.5 문자열 맨 앞의 공백이 널바이트로 저장됐다 ★
`_JONG = " ㄱㄲㄳ…"` (0번 = 받침 없음) 의 선행 공백이 파일에 `\x00` 으로 들어가서
파이썬이 모듈을 아예 못 읽었다.
→ **리스트의 빈 문자열**(`["", "ㄱ", …]`)로 바꿔서 원인을 없앴다.
→ 같은 표가 세 군데 복사돼 있던 것도 `sheet.split()` 하나로 합쳤다.

### 3.6 f-string 안의 자바스크립트 ★
지도의 JS 를 f-string 안에 두었더니 `{ }` 가 전부 치환 자리로 읽혀 빌드가 죽었다.
→ JS 본문은 **f-string 밖**(`build_map.JS`)에 두고 상수만 f-string 으로 만든다.

### 3.7 문장이 두 줄로 넘어가 「요!」가 외톨이가 됐다 ★
원고지 칸을 고정 크기로 두었더니 13칸짜리 문장이 두 줄로 접혔다. 아이는 그게
같은 문장인지 모른다.
→ 칸 수에 맞춰 **칸 크기를 계산**하고, 12mm 아래로 내려가면 빌드를 실패시킨다
   (그때는 문장을 줄여야 한다).

### 3.8 지도의 마당 설명이 개발자 말투였다 ★
`units.json` 의 `note` 를 그대로 지도에 실었더니 "원본 PDF와 같은 양식이다",
"급하게 넘기지 말 것" 같은 문장이 **아이·부모 화면에** 나왔다.
→ `note` 는 부모가 읽는 말로 쓴다. 만든 사람의 판단 근거는 이 문서에 쓴다.

### 3.9 GitHub — Authorize 와 Install 은 다르다 ★★★
(영어 저장소에서 겪은 것. 여기서도 **저장소 생성이 403**이었다.)
GitHub App 은 사용자 계정에 **저장소를 새로 만들 권한이 없다.** 사람이 만들어야 한다.
푸시가 403이면 https://github.com/apps/claude 에서 **Install** 까지 됐는지 본다.
→ 진단: `curl .../info/refs?service=git-receive-pack` 이 403이고
   `git-upload-pack` 이 200이면 이 경우다.

### 3.10 Pages 는 손으로 한 번 켜야 한다 ★★
워크플로에 `enablement: true` 가 있어도 워크플로 토큰에는 Pages 를 처음 켤
권한이 없어서 `Resource not accessible by integration` 으로 실패한다.
→ Settings → Pages → Source: **GitHub Actions**. 한 번만.
→ 빈 저장소에 처음 푸시한 브랜치가 기본 브랜치가 되고, Pages 는 거기서만 배포된다.

---

## 4. 검사기 이야기

`check.py` 는 9가지를 본다. 그중 **8번(아직 안 배운 글자)** 이 이 저장소의 값어치다.

「차근차근」이라고 써 놓고 20번 학습지에 겹받침이 나오면 그 말이 거짓이 된다.
사람은 44장을 훑다가 반드시 놓친다. 실제로 **「왕관」의 `ㅘ`** 를 내가 놓쳤고
검사기가 잡았다.

일부러 새 글자를 넣어야 할 때는 `units.json` 에 `"new": ["ㅔ"]` 라고 적는다.
적으면 학습지 머리말에 **「새 글자 ㅔ(에)」** 딱지가 붙는다 — 아이가 못 읽는 게
당연하다는 표시다. 안 적으면 빌드가 막힌다. 지금 선언된 것은 6자:

| 유닛 | 글자 | 왜 |
|---|---|---|
| 29 드레스 | ㅔ | 공주 테마에 꼭 필요한 낱말 |
| 32 왕관 | ㅘ | 위와 같음 |
| 39 꽃 | ㄲ | ㅊ받침을 가르치는 가장 흔한 낱말 |
| 40 나는 공주예요 | ㅖ | 문장 어미 |
| 41 …왕관을 써요 | ㅆ | 문장 어미 |
| 44 …한글을 읽어요 | ㄺ | 마지막 장의 겹받침 |

**검사기를 고쳤으면 반드시 일부러 깨뜨려서 실패하는지 본다.**
이번에 세 가지로 확인했다 — 안 배운 글자 넣기 / 학습지 파일 치우기 / 지도 칸 지우기.
0개를 세고 "이상 없음" 을 내는 검사기가 제일 위험하다. 그래서
`fonts.py --check` 는 한글을 100자 미만으로 찾으면 통과시키지 않고,
`check.py` 는 지도의 칸 개수를 유닛 수와 대조한다 (지도는 링크를 JS 가 만들어서
정규식으로 훑으면 0개가 나온다).

---

## 5. 레이아웃 검증

`build.py` 가 **Chromium 으로 실제 인쇄 PDF 를 뽑아 쪽 수를 센다.** 한 장이 아니면
빌드가 실패한다. 마당 묶음(`madangNN.html`)도 장수와 쪽 수를 대조한다.

왜 Chromium 인가 — 부모가 브라우저에서 인쇄 버튼을 누른 결과가 정답이기 때문이다.
weasyprint 같은 다른 엔진으로 재면 실제 인쇄와 어긋난다.

느리다(44장에 65초). 그래도 끄는 선택지는 안 뒀다. 검증을 끄면 언젠가 두 장짜리가
인쇄되고, 그때는 이미 아이가 앞에 앉아 있다.

넘쳤을 때: `python3 tools/where_overflow.py print/unitNN.html` 이 어느 덩어리가
몇 mm 넘겼는지 짚어 준다. 덜어내는 순서는 **부모용 안내 → 색칠 도안 → 쓰는 칸**.
쓰는 칸은 마지막이다 — 6살 손에는 칸이 커야 한다.

---

## 6. 글꼴을 왜 파일에 박았나

인쇄용 저장소다. 인쇄 버튼을 누르는 순간 인터넷이 느리면 웹폰트가 안 붙고,
그러면 **글자 크기가 달라져서 A4 한 장이 두 장이 된다.** 따라쓰기 칸 안의 회색
글자가 칸 밖으로 삐져나가기도 한다.

그래서 쓰는 글자(653자)만 남긴 서브셋을 base64 로 `assets/fonts.css` 에 박았다.
325KB. 한글 전체를 넣으면 5MB 가 넘는다.

원본 ttf 는 `.build/fonts/` 에 캐시하고 저장소에는 안 넣는다 (17MB).
다시 만들려면 인터넷이 필요하다 — `python3 tools/fonts.py`.

---

## 7. 도장(진도) 저장 방식

영어 저장소와 같다.

- `units.json` 의 `status:"done"` → 지도에 **미리 찍혀 있는 도장**. 어느 기기에서나 같다
- 브라우저 `localStorage` → 아이가 **직접 찍은 도장**. 그 기기에만

저장할 때 **그때의 씨앗 목록을 함께** 남겨 두고, 다음에 열 때 그 사이 새로
`done` 이 된 것만 더한다. 그래서 `units.json` 을 나중에 고쳐도 아이가 일부러 뺀
도장이 되살아나지 않는다.

`localStorage` 접근 자체가 막힌 브라우저가 있으니 읽기·쓰기를 전부 try/catch 로
감쌌고, 저장이 안 되면 화면 위에 빨간 띠로 알린다.

기기 사이 동기화는 안 된다. `list.html` 의 「진도 복사하기」로 번호를 뽑아
`units.json` 에 옮겨 적는 게 유일한 길이다.

---

## 8. 팔레트

```
--ink   #3B2B40   진한 보라회색 — 글자·테두리
--rose  #E0508A   분홍 — 머리말 탭·강조
--gold  #E8A33D   금색 — 굵은 밑줄
--lilac #A98BD0   연보라 — 보조·안내
--line  #CFC2D8   연한 선
--trace #D6D2DA   따라쓰기 회색 글자
--guide #E6E0EC   칸 안 십자 보조선
글꼴    Jua(제목) · Gaegu(손글씨 안내) · Noto Sans KR(따라쓰기 글자·본문)
```

**따라쓰기 글자에는 둥근 글꼴을 쓰지 않는다.** 아이가 그대로 베낄 모양이라
표준적인 자형이어야 한다. Jua·Gaegu 는 제목과 안내에만 쓴다.

**바탕을 칠하지 않는다.** 전면 배경색은 잉크를 먹고 크레파스가 안 먹는다.

---

## 9. 남은 일

- **교재 대조.** 사용자가 가진 『2021 한글한마당(학생교재)』와 커리큘럼 순서·낱말
  선정을 맞춰 보지 못했다. 구글드라이브가 이그레스 정책에 막혀 받지 못했고
  (`drive.usercontent.google.com` CONNECT 403), 대화 첨부로만 들어올 수 있다.
  교재를 보면 `units.json` 하나만 고치면 전체가 따라오게 짜 두었다.
- **겹자음·복합모음 마당이 없다.** ㄲㄸㅃㅆㅉ 과 ㅐㅔㅘㅚ… 는 지금 `new` 로 여섯 자만
  땜질하고 있다. 아이가 44장을 끝내면 7마당으로 제대로 만들어야 한다.
- **획순 애니메이션.** 종이가 목적이라 안 만들었지만, 지도의 쪽지에서 획이 순서대로
  그려지는 걸 보여 주면 부모가 가르치기 쉬워진다 (`jamo.py` 에 좌표가 이미 있다).
- **도안이 36개뿐**이다. 낱말을 늘리려면 그림부터 늘려야 한다.

> 그리고 영어 자료에서 배운 것 하나 — **자료를 더 만드는 것보다 아이와 다음 한 장을
> 하는 게 우선이다.** 영어는 52권을 만들어 놓고 아이는 13권에서 39일 멈춰 있었다.
